PORT=4000
APP_PORT=80

# Postgres (used by docker compose and the API)
POSTGRES_DB=crablearn
POSTGRES_USER=crablearn
POSTGRES_PASSWORD=change-me
PGHOST=127.0.0.1
PGPORT=5432
PGUSER=crablearn
PGPASSWORD=change-me
PGDATABASE=crablearn

# Frontend (empty = same-origin API when served by Express/Docker)
# VITE_API_BASE_URL=
